<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BeeOpen"
             x:Class="BeeOpen.MainPage"
             BackgroundColor="#274156">

    <Grid Padding="20">
    
        <Grid.RowDefinitions></Grid.RowDefinitions>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Padding="30">
            <Image x:Name="Image" Source="logowhite.png"/>
            <Entry x:Name="username" Placeholder="Όνομα Χρήστη" PlaceholderColor="#fbfcff"   TextColor="#fbfcff" />
            <Entry x:Name="password" Placeholder="Κωδικός Πρόσβασης" PlaceholderColor="#fbfcff" TextColor="#fbfcff"/>
            <Button x:Name="login" BackgroundColor="#1c6e8c" Text="Σύνδεση" TextColor="#fbfcff"  Clicked="Login_Clicked" />
        </StackLayout>

        <StackLayout VerticalOptions="End" HorizontalOptions="Center">
            <Button x:Name="Register" Text="Έγγραφή" TextColor="#fbfcff" BackgroundColor="Transparent" BorderColor="Transparent" Clicked="Register_OnClicked"></Button>
        </StackLayout>

    </Grid>
   
</ContentPage>
