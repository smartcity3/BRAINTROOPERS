<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BeeOpen"
             x:Class="BeeOpen.MainPage"
             BackgroundColor="#d0ccd0">

    <Grid>

        <Grid.RowDefinitions></Grid.RowDefinitions>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Padding="30">
            <Entry x:Name="username" Placeholder="Όνομα Χρήστη" TextColor="#605856" />
            <Entry x:Name="password" Placeholder="Κωδικός Πρόσβασης" TextColor="#605856"/>
            <Button x:Name="login" BackgroundColor="#274156" Text="Σύνδεση" TextColor="#fbfcff"  Clicked="Login_Clicked" />
        </StackLayout>

        <StackLayout VerticalOptions="End" HorizontalOptions="Center">
            <Button x:Name="Register" Text="Έγγραφή" BackgroundColor="Transparent" BorderColor="Transparent" Clicked="Register_OnClicked"></Button>
        </StackLayout>

    </Grid>
   
</ContentPage>
